export interface AsideItem {
  id: string;
  class: string;
  action: string;
  svg: string;
  title: string;
  isActive: boolean;
}

export interface CardVideo {
  name: string;
  coverVideo: string;
}
